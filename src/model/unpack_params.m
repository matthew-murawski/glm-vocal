function kernels = unpack_params(wmap, colmap, cfg, stim)
%UNPACK_PARAMS Placeholder unpacking of parameter vector.
%   kernels = UNPACK_PARAMS(wmap, colmap, cfg, stim) returns an empty struct.

unused = {wmap, colmap, cfg, stim}; %#ok<NASGU>
kernels = struct();
end
