function mu = predict_rate(X, w)
%PREDICT_RATE Placeholder computation of firing rate predictions.
%   mu = PREDICT_RATE(X, w) returns a zero-valued rate vector.

unused = {w}; %#ok<NASGU>
mu = zeros(size(X, 1), 1);
end
