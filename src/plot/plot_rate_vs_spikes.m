function plot_rate_vs_spikes(stim, y, mu, outdir)
%PLOT_RATE_VS_SPIKES Placeholder rate-versus-spikes plotter.
%   PLOT_RATE_VS_SPIKES(stim, y, mu, outdir) performs no plotting in the stub.

unused = {stim, y, mu, outdir}; %#ok<NASGU>
end
