function plot_kernels(kernels, outdir)
%PLOT_KERNELS Placeholder kernel plotting routine.
%   PLOT_KERNELS(kernels, outdir) performs no plotting in the stub.

unused = {kernels, outdir}; %#ok<NASGU>
end
