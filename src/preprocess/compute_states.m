function states = compute_states(ev, stim, stateCfg)
%COMPUTE_STATES Placeholder conversational state computation.
%   states = COMPUTE_STATES(ev, stim, stateCfg) returns empty state flags.

unused = {ev, stim, stateCfg}; %#ok<NASGU>
states = struct('state_convo', [], 'state_spon', []);
end
