function sps = bin_spikes(spike_times, stim)
%BIN_SPIKES Placeholder binning of spike times.
%   sps = BIN_SPIKES(spike_times, stim) returns an empty column vector.

unused = {spike_times, stim}; %#ok<NASGU>
sps = zeros(0, 1);
end
