function streams = build_streams(ev, stim)
%BUILD_STREAMS Placeholder builder for binary event streams.
%   streams = BUILD_STREAMS(ev, stim) returns empty stream shells.

unused = {ev, stim}; %#ok<NASGU>
streams = struct('heard_any', [], 'produced_any', []);
end
