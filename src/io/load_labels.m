function ev = load_labels(pathOrPaths)
%LOAD_LABELS Placeholder loader for event label data.
%   ev = LOAD_LABELS(pathOrPaths) returns produced/heard shells.

ev = struct('produced', [], 'heard', [], 'source', pathOrPaths);
end
