function [D, Dmap] = smoothness_penalty(colmap, cfg)
%SMOOTHNESS_PENALTY Placeholder for smoothness penalty construction.
%   [D, Dmap] = SMOOTHNESS_PENALTY(colmap, cfg) returns empty penalty shells.

unused = {colmap, cfg}; %#ok<NASGU>
D = sparse([], [], [], 0, 0);
Dmap = struct();
end
