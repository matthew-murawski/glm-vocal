function [Xblk, info] = build_history_block(sps, stim, window_s)
%BUILD_HISTORY_BLOCK Placeholder builder for spike-history blocks.
%   [Xblk, info] = BUILD_HISTORY_BLOCK(sps, stim, window_s) returns empty outputs.

unused = {sps, stim, window_s}; %#ok<NASGU>
Xblk = sparse([], [], [], 0, 0);
info = struct();
end
