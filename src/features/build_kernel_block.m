function [Xblk, info] = build_kernel_block(z, stim, window_s, mode)
%BUILD_KERNEL_BLOCK Placeholder builder for stimulus kernel blocks.
%   [Xblk, info] = BUILD_KERNEL_BLOCK(z, stim, window_s, mode) returns empty outputs.

unused = {z, stim, window_s, mode}; %#ok<NASGU>
Xblk = sparse([], [], [], 0, 0);
info = struct();
end
